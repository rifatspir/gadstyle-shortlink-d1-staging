Patch: step05
Project: gadstyle-shortlink-d1-staging
Baseline: app-patch-step04
Type: patch-only

Changed files:
- app/page.tsx
  - Logs lightweight smart-download events before Android/iOS redirects and desktop/unknown fallback rendering.
  - Uses SHORTLINK_API_BASE_URL and optional SMART_DOWNLOAD_LOG_SECRET without blocking the public page beyond a short timeout.
  - Does not log preview/bot user agents.
- components/AdminDashboardClient.tsx
  - Adds Smart download report panel to the admin dashboard.
  - Shows total root requests, Android store redirects, App Store redirects, fallback views, and top sources.
- lib/worker-admin.ts
  - Extends admin stats typing for smart-download report data.
- app/globals.css
  - Adds responsive dashboard styles for the smart-download report cards and source table.
- .env.example
  - Adds optional SMART_DOWNLOAD_LOG_SECRET placeholder.
- PATCH_MANIFEST.txt
  - Updated for step05.

Unchanged behavior / safety:
- No shortlink route behavior changed.
- No product/category/brand direct resolver behavior changed.
- No admin authentication/session behavior changed.
- No D1 write is performed on preview/bot traffic.
