Patch: step08
Project: gadstyle-shortlink-d1-staging
Baseline: gadstyle-shortlink-d1-staging-step07-stable-full.zip
Type: patch-only

Changed files:
- app/page.tsx
  - Forced dynamic/no-store rendering for the root smart-download landing page.
  - Increased server-side mobile redirect logging timeout from 650ms to 2000ms.
  - Logs Android/iOS store redirects server-side before redirecting.
  - Uses a client-side fallback logger for desktop/unknown landing views so fallback report counts update reliably even when the landing page is shown.
- app/api/download-events/route.ts
  - Added a lightweight public Vercel proxy endpoint for fallback landing-view reporting only.
  - Forwards sanitized fallback events to the Worker without exposing SMART_DOWNLOAD_LOG_SECRET to the browser.
- components/SmartDownloadFallbackLogger.tsx
  - Added tiny client component using sendBeacon/fetch keepalive to record desktop/unknown fallback landing views.
- PATCH_MANIFEST.txt
  - Updated for step08.

Unchanged behavior / safety:
- Existing /admin, /p, /c, /b, and /s routes are unchanged.
- Android/iOS smart redirect behavior remains unchanged.
- No IP address or full user-agent is stored.
