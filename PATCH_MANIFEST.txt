PATCH: Phase 5
BASELINE: gadstyle-shortlink-d1-staging-phase4-fix2.zip
APPLIED_DIRECTLY_ON_TOP_OF_PREVIOUS_STABLE: YES

MODIFIED FILES:
- lib/env.ts
- .env.example
- README.md
- DEPLOYMENT_GUIDE.md
- BUILD_NOTES.txt
- PATCH_MANIFEST.txt

ADDED FILES:
- none

DELETED FILES:
- none

SUMMARY:
- Prepared the frontend production cutover package for Vercel -> Worker -> D1.
- Relaxed env bootstrap so Worker+D1 mode can run without hard-requiring DATABASE_URL when SHORTLINK_API_BASE_URL is present.
- Kept route contract unchanged: /p/{id}, /c/{id}, /b/{id}, /s/{code}.
- Recommended keeping Neon env for first rollout so rollback stays simple.
