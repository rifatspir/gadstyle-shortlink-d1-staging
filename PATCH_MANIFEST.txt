PATCH: Phase 1 Flutter shortlink share fix support
BASELINE: gadstyle-shortlink-service-phase5-production.zip
APPLIED_DIRECTLY_ON_TOP_OF_PREVIOUS_STABLE: YES

MODIFIED FILES:
- PATCH_MANIFEST.txt

ADDED FILES:
- app/api/shortlinks/resolve-direct/route.ts

DELETED FILES:
- none

SUMMARY:
- Added a public resolve-direct API route on the Vercel frontend so Flutter can check for an existing Worker+D1 shortlink by entity type/id before requesting create.
- Kept public route contract unchanged: /p/{id}, /c/{id}, /b/{id}, /s/{code}.
- Kept Worker + D1 architecture unchanged and did not reintroduce Neon.
