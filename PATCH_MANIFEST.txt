Phase 3 - Easier admin creation + branding polish

Modified files:
- app/layout.tsx
- app/globals.css
- app/login/page.tsx
- components/Header.tsx
- components/LinkForm.tsx

Added files:
- app/icon.svg
- app/apple-icon.svg
- app/favicon.ico

Notes:
- Canonical URL paste now auto-detects target type, numeric ID, display slug, app path, and default code style.
- Routing contract remains unchanged and ID-based.
- Branding polish added with lightweight in-app vector assets only; no heavy external image assets added.
- Worker and Flutter app unchanged in this phase.


Phase 3 compile hotfix:
- Escaped JSX text issue in LinkForm supported patterns hint by using plain brace text.
- No routing or API contract changes.
