Build: phase5-admin-security-phase1-2

Modified files:
- app/login/page.tsx
- app/globals.css
- app/api/auth/login/route.ts
- lib/auth.ts
- lib/env.ts
- lib/session.ts
- .env.example

Added files:
- lib/totp.ts
- lib/login-guard.ts
- app/login/verify/page.tsx
- app/api/auth/verify-2fa/route.ts

Behavior summary:
- moved login credit into login card just below the login button
- shortened admin session duration and kept secure cookie flags
- added generic-failure login guard with temporary lockout after repeated failures
- added TOTP two-factor verification compatible with Google Authenticator/Authy
- added recovery-code support through environment configuration
- added environment-based 2FA bypass/reset compatibility for emergency recovery

Notes:
- public routes /p/{id}, /c/{id}, /b/{id}, /s/{code} are unchanged
- worker and D1 behavior are unchanged
- website frontend is untouched
